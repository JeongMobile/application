<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.eunjung.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Register"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".addLists" />
        <activity android:name=".makeGroup" />
        <activity android:name=".showMember" />
        <activity android:name=".MainActivity" />
        <activity
            android:name="."
            android:label="@string/title_activity_todo_lists"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".showTodoLists"
            android:label="@string/title_activity_show_todo_lists"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Test"
            android:label="@string/title_activity_test"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DetailTodo"
            android:label="@string/title_activity_detail_todo"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>
